slam_toolbox:
  ros__parameters:
    use_sim_time: false

    # Minimal, commonly-supported parameters (kept intentionally small
    # to reduce cross-version incompatibilities).
    mode: mapping
    map_frame: map
    odom_frame: odom
    base_frame: base_link
    scan_topic: scan
